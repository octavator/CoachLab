defmodule Reservation do
    """
    data = %{
        price: 100,
        currency: euro,
        
    }
    """
    def create_resa(coach_id, user_ids, data) do
        
    end

    def confirm_resa(coach_id, reservation_id) do
        true
    end

    def delete_resa(coach_id, reservation_id) do
        true
    end

    
end